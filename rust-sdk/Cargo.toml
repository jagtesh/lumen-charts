[package]
name = "lumen-charts-sdk"
version = "0.1.0"
edition = "2021"
description = "Safe, idiomatic Rust SDK for Lumen Charts (v5 API compatible)"
publish = false

[dependencies]
lumen-charts-core = { path = "../core" }
serde_json = "1"
